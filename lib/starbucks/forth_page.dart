import 'package:flutter/material.dart';

class StarbucksForthPage extends StatelessWidget {
  const StarbucksForthPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: Center(
        child: Text("Starbucks 네 번째 페이지"),
      ),
    );
  }
}
